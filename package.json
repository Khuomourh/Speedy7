{
  "name": "speedy7-preview",
  "version": "1.0.0",
  "private": true,
  "description": "Speedy7 car parts search and quote app preview.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check app.js && node --check server.js && node --check api/index.js && node --check service-worker.js",
    "import:parts": "node scripts/import-parts.js"
  },
  "keywords": [
    "speedy7",
    "auto-parts",
    "quotes",
    "supabase"
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
